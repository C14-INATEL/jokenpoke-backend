services:

  # ========================================
  # API
  # ========================================
  api:
    build: 
      context: .
      dockerfile: docker/Dockerfile
    container_name: jokenpoke-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_ALGORITHM=${JWT_ALGORITHM}
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - jokenpoke-net
    restart: always

  # ========================================
  # CI/CD
  # ========================================
  jenkins:
    image: ${DOCKER_HUB_USER}/jokenpoke-jenkins:latest
    pull_policy: always
    container_name: jokenpoke-jenkins
    ports:
      - "8080:8080"
      - "50000:50000"
    group_add:
      - "0"
    volumes:
      - jenkins_data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/workspace:ro
    environment:
      JAVA_OPTS: >-
        -Djenkins.install.runSetupWizard=false
        -Dhudson.model.DirectoryBrowserSupport.CSP=
      SONAR_HOST_URL: http://sonarqube:9000
    depends_on:
      - sonarqube
    networks:
      - jokenpoke-net
    restart: always

  # ========================================
  # QUALIDADE DE CÓDIGO
  # ========================================
  sonarqube:
    image: sonarqube:community
    container_name: jokenpoke-sonarqube
    ports:
      - "9000:9000"
    environment:
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_logs:/opt/sonarqube/logs
      - sonarqube_extensions:/opt/sonarqube/extensions
      - ./docker/sonarqube/sonar-project.properties:/opt/sonarqube/conf/sonar-project.properties:ro
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    networks:
      - jokenpoke-net
    restart: always

  # ========================================
  # TUNNEL
  # ========================================
  ngrok:
    image: ngrok/ngrok:latest
    container_name: jokenpoke-ngrok
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    command: "http --domain=${NGROK_DOMAIN} jenkins:8080"
    ports:
      - "4040:4040"
    depends_on:
      - jenkins
    networks:
      - jokenpoke-net
    restart: always

volumes:
  jenkins_data:
  sonarqube_data:
  sonarqube_logs:
  sonarqube_extensions:

# ========================================
# REDE COMPARTILHADA
# ========================================
networks:
  jokenpoke-net:
    driver: bridge